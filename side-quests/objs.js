// Create an object that represents a Harry Potter character.
// It should contain at least one boolean, one string, one number, and one array
// Log out at least two of the keys using the dot notation

let harryPotter = {
  name: "Harry Potter",
  isWizard: true,
  wand: 11,
  spells: ["Lumos", "Expelliarmus", "Expecto Patronum"]
}

console.log(harryPotter.name)
console.log(harryPotter.spells)
